# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Secrets — never commit. .env.example is the only env file that is tracked.
.env
.env.*
!.env.example
dev-dist

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# ORS response cache for the one-off route build; not build output.
.ors-cache

# Copied from node_modules by prebuild; not source.
public/maplibre
